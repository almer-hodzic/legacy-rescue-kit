{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "builds": [
    {
      "src": "src/Frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/legacy-kit-ui/browser" }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}

